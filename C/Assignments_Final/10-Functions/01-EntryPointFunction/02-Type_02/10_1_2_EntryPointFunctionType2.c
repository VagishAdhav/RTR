#include <stdio.h> //contains declaration of printf

// entry  point function => main() => Valid return type(int) and No parameters (void),
// Hence no command line argument

int main(void)
{
    //code
    printf("Hello world !!!\n"); // library function
    return (0);
}

